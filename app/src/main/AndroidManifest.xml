<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.team04.crucible">

    <application
        android:allowBackup="true"
        android:icon="@drawable/Card_Crucible_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CardCrucible">
        <activity android:name=".EditThemeActivity"></activity>
        <activity android:name=".GameModeActivity" />
        <activity android:name=".AddCardsActivity" />
        <activity android:name=".CardSelectionActivity" />
        <activity android:name=".EditCardsActivity" />
        <activity android:name=".EditCardSelectionActivity" />
        <activity android:name=".StudyModeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
    </application>

</manifest>